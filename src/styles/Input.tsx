import styled from "@emotion/styled";

export const Input = styled.input`
  color: #222;
  border: none;
  outline: none;
  background: transparent;
  font-size: 16px;
  padding: 0;
  margin: 0;
  &:focus {
    outline: none;
  }
`;
